{
  "name": "ts",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "bun build --watch ./components/*.ts --outdir ../../public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
